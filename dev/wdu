#!/usr/bin/env bash
# wdu — run the runtime waydroid checkout as the user (session/UI cmds; needs
# the Wayland env). Usage: dev/wdu session start | dev/wdu show-full-ui
source "$(dirname "$(readlink -f "$0")")/env.sh"
exec "${WAYDROID[@]}" "$@"
